spring.datasource.url=jdbc:postgresql://containers-us-west-49.railway.app:${PGPORT}/${PGDATABASE}
spring.datasource.username=${PGUSER}
spring.datasource.password=${PGPASSWORD}

spring.sql.init.mode=always